apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: telegraf-sa-fms-data
  name: telegraf-sa-fms-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
